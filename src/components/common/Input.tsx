/**
 * TODO: Make this a reusable input component.
 */

const Input = () => {
  return (
    <input
      aria-label="input"
      className="ml-2 rounded-md px-1 outline outline-slate-100"
      type="number"
    />
  );
};

export default Input;
